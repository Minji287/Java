package package_2;

public class Card {
	// 매개 변수
	String kind; // instance 변수
	int number;
	static int width = 100; // class 변수
	static int height = 250; // static이 붙으면 메인에 가져가서 쓸 수 있음(객체 생성 안해도 됨)
}
